# Copie para .env.local (não versionado) e ajuste conforme o ambiente.
# Endereço da API — sem barra no final.
#
# Portas de desenvolvimento da fin-flower-api:
#   http://localhost:5212   (sem TLS, não exige confiar no certificado de dev)
#   https://localhost:7046  (exige: dotnet dev-certs https --trust)
VITE_API_URL=http://localhost:5212
